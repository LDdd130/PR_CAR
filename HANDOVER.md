# PR_CAR 인수인계 (HANDOVER)

> STM32F411 + FreeRTOS 차동구동 벽추종 자율주행 RC카.
> 최종 갱신: **2026-07-14 (§5.13, IMG_3044↔mappv8 안정화)** — 실주행 영상+웹앱 텔레메트리 교차분석 기반 수정 반복 중.
> 표기: 코드로 검증 못 한 내용은 **[추정]**. 코드/diff 사실이 세션 대화보다 우선.

---

## 0. TL;DR — 다음 에이전트가 알아야 할 것

1. **아키텍처 (§5.17 v2 재작성)**: `drive.c` 단일 코어 — CRUISE(센터링+속도 거버너) / TURN(아크
   주, 피벗 폴백) / RECOVER(브레이크·후진·측면회피) 3계층, 텔레메트리 7상태 enum은 불변.
   `drive_control.c/.h`는 **삭제됨** (drive.c에 통합). `drive_config.h`(**모든 튜닝 노브** +
   `#error` 가드) / `drive_math.h`(유틸) / `motor.c`(단일 모터 경로).
   **측면 ToF는 90°(직각) 장착 전제** — 측면 기하 상수는 도면 유도값, [재실측] 필요.
2. **현재 상태**: IMG_3044↔mappv8에서 최고속도 상향 자체는 확인됐지만, 코너/회피 직후 잔류 회전과
   SIDE_AVOID 과회전, SPIN 중 지속 전방위험 무시가 연쇄 충돌을 만들었다. §5.13에서 최고속도는 유지하고
   불안정 구간만 동적 감속·각도 제한·지속위험 후진으로 수정 — **Debug/Release 빌드 검증, 실차 미검증**.
   목표: 무충돌 완주 우선, 이후 20s 이내.
3. **작업 방식**: 사용자가 주행영상(iPhone) + 웹앱 화면녹화를 거의 동시에 찍어 줌 →
   §6의 교차분석 워크플로로 오발 프레임을 특정 → 근본원인 수정. **증상 패치 금지, 근본원인만**
   (사용자 강한 선호). 하드웨어 문제면 코드로 덮지 말고 하드웨어라고 말할 것.
4. 빌드: `make -j8` / `make RELEASE=1 -j8` → `build[/release]/PR_CAR.hex`. 유일한 회귀 테스트 = 실차.
5. §7(재발 방지)과 §9(불변식)를 코드 수정 전에 반드시 읽을 것.
6. 작업 트리는 원래부터 대규모 dirty/untracked 상태다. **`git reset --hard`, `git clean` 금지.**
7. 코드 수정 후 `graphify update .` 실행 (AST-only, 무료).

---

## 1. 시스템 개요

- MCU STM32F411(Cortex-M4) + FreeRTOS(CMSIS-OS2). IWDG 2.048s(큐 수신 성공 시에만 refresh).
- 센서: HC-SR04 전방(TIM3 CH1 입력캡처, **에코 대기 6ms = 최대 ~1m, 초과 시 f_valid=0인데
  대시보드 F=80으로 표시됨**), VL53L0X 측면 ToF ×2(I2C1, 좌측 0x60 재배치), BNO055 IMU(heading,
  부팅 시 180° 재영점, **모터 인러시 브라운아웃으로 기동 순간 죽을 수 있음**),
  SG-207 휠 엔코더 ×2(TIM2 — delay_us와 공유, CNT 리셋 금지; 제어 미사용, 텔레메트리 전용.
  **우측 엔코더 전 구간 0 출력 — 하드웨어 점검 필요**).
- 액추에이터: L298N + TT모터 4륜 (TIM4 ch1=우/ch2=좌). **직진 스톨 하한 30% duty.
  피벗(스키드 조향)은 돌파 듀티가 그보다 훨씬 높음** — 4바퀴 횡슬립.
- 통신: HM-10 BLE(USART1 9600). 웹앱 `docs/monitor_web_app/index.html`.
- 태스크: SensorTask(측정→`driveQ` full frame + 12cm 미만 front-only 비상 이벤트) →
  MotorTask(`Drive_Update` 소비, IWDG refresh) / BluetoothTask(RX 명령 + TX 텔레메트리 10Hz).
- 차체 27×16cm. 트랙(testtrack.drawio): 폭 37→45→43→(45°챔퍼×2, 55)→50→**67(방지턱)**→50→60cm.
- 코스 축: 출발 북(0°) → 첫 직각 우회전(동 +90°) → 둘째 직각 좌회전(북 0°) → 넓고 긴 직선 →
  곡선 우회전 → … → 마지막 방지턱 전 직각: 동(+90°)→남(180°) 우회전 (좌우 대칭이라 기하 판정 불가,
  코스축 폴백 전용).

### 실측 센서 값 (사용자 수동주행, 코드 단위 cm — 판정 기준의 근거 데이터)

| 위치 | 실측 | 기대 판정 |
|---|---:|---|
| 초기 좁은 직선 | L=20, R=15 전후 | 직선 (코너 아님) |
| 첫 직각 코너 | F≈20, L=23, R=36 | right |
| 둘째 직각 코너 | L=33, R=17 | left |
| 넓고 긴 직선 | L=24, R=19~20 | 직선 |
| 곡선 진입 | F≈26, L=19~20, R=46~50 | right |
| 방지턱 전 마지막 직각 | L≈R, F≤34 | 코스축 폴백 right |
| **긴 직선 치우침 시 (오발 소스)** | far측 28.6~33.3 관측 | 직선 — 코너로 보면 안 됨 |

- 직진 중에도 진동으로 heading이 몇 도씩 튀는 게 정상 — 과하게 잡으면 안 됨 (사용자 확인).
- 트랙 측면 여유 ~10cm — 벽에 닿는 문제는 속도를 낮춰 풀지, 거리 문턱을 올려 풀지 말 것.

### 상태머신 (drive.c) — 텔레메트리 `st`와 1:1
`DS_CRUISE(0) → DS_BRAKE(1) → DS_SPIN(2) / DS_REVERSE(3) / DS_HOLD(4) / DS_SIDE_AVOID(5) / DS_CORNER(6)`
- 정상 코너 = `0→6→0` (전진 아크). 피벗(2)은 폴백. `0→1→2` 반복이면 코너 판정 문제.
- BRAKE는 §5.9부터 "벽 검증" 단계: 스파이크면 크루즈 복귀, 진짜 벽이면 20cm까지 크립 후 좌우 판정.
- front-only 프레임(side_valid=0)은 12cm 비상 제동 전용 — FSM 카운터 오염 금지.

### CRUISE 조향 (drive.c `centering_run`) — 2단 캐스케이드 (§5.17에서 drive.c로 통합)
- 외루프: 횡오차(l−r) P/D → 목표 heading 오프셋 (캡 ±11°, knee 10cm/+0.8). **|L−R|<3cm는
  진짜 불감대** (ToF를 0.5cm까지 못 믿으므로 미세 센터링 안 함 — 사용자 방침).
- 내루프: heading 오차(**1-pole LPF α=0.25로 필터, deadband는 0.6° 채터방지만**, knee 10°/+0.6)
  → 차동 duty + yaw-rate 댐핑(LPF 0.22, deadband 8dps, cap 6%).
- ref 무관 보조력: 10cm 점진 벽 반발(직각 ToF 기준, [재실측]).
- **heading_ref는 `course_axis_snap()`(90° 단위)만 사용.** 45° 그리드는 v2에서 완전 삭제.
- **속도 거버너 = 1급 규칙**: 전방거리 캡(30cm→34%, 75cm→60%)이 제동·판정 예산을 보장.
  측면/헤딩오차/yaw-rate/settle 캡 중 최솟값. 상한 60% (v1 63-69에서 하향 — 안정 우선).

---

## 2. 히스토리 요약 (전체 상세는 git 히스토리의 본 문서 이전 판)

| 단계 | 내용 | 실차 결과 |
|---|---|---|
| Codex 리팩터링 | 모듈 분리 (drive.c 1,609줄 → 4파일) | — |
| Claude 1~3차 | 피벗 스톨/스키드 돌파 듀티, front 타임아웃 탈출(`front_open_at`), turn_progress 누적화 | 개선 확인 |
| Claude 4~5차 + Codex 6차 | 벽 반발 상시화, `mix_substall`, 직선 필터/속도 완화 | 부분 개선 |
| Codex 7차 | 실측값 기반 구간별 코너 기준 + 대칭 코너 코스축 우회전 폴백 | 직진 위빙 (IMG_3028) |
| Claude 6~7차 (§5.7~5.8) | 위빙: deadband 재구성 2회 실패 → **헤딩오차 LPF + deadband 0.6° + 측면 3cm 불감대**로 해결 | 위빙 해소 |
| Claude 8차 (§5.9) | 전방 스파이크 오발턴 → BRAKE=벽검증 + **20cm 크립 확정 후 좌우 판정** | 턴 이후 사행 (IMG_3029) |
| Claude 9차 (§5.10) | 교차분석: **ref 45°대각 스냅이 주범** → 90° 축 스냅, 코너 게이트 68→44, TURN_MIN 45→30 | 대폭 개선 (IMG_3030) |
| Claude 10차 (§5.11) | 긴 직선 오발턴 + SIDE_AVOID 데드락 (아래 §5.11) | **미검증** |
| Codex 11차 (§5.12) | IMG_3032↔mappv3: wide→normal 폴백·미사용 graze 게이트 수정, 확정 중 롤링 아크, wide 속도 -4.5% | **빌드 통과, 실차 미검증** |

---

## 3. 파일 맵 + 현재 노브 값 (2026-07-14 기준)

| 파일 | 역할 |
|---|---|
| `Core/Inc/drive_config.h` | **모든 튜닝 상수** + `#error` 불변식 가드 (§5.17 전면 재작성) |
| `Core/Src/drive.c` | v2 코어 전체: FSM + 센터링 + 속도 거버너 + 진행각 누적 (§5.17 재작성, ~1000줄→구 대비 노브 절반) |
| `Core/Src/motor.c` | `Motor_SetWheels`, 방향 반전 1ms 중립, `Car_Brake` |
| `Core/Src/freertos.c` | 3태스크, 센서 필터/유효성, BLE 프로토콜 |

자주 만지는 노브 (전부 drive_config.h):
- 피벗: `TURN_SPEED 56 / TURN_INNER 34`, PID 하한 `44/40`, `TURN_MIN_DEG 30`, `TURN_TARGET_DEG 88`
- 코너 아크: `ARC_OUTER 72 / ARC_INNER 30`, 구조 피벗 `CORNER_RESCUE 48/-32` (SIDE_AVOID 탈출도 이것)
- 전방 안전거리: danger/abort/**decide**/stop/turn/clear/arc = `12/16/20/34/44/52/68cm`
- 코너 판정: **전방 44cm 3프레임 연속확정(`FRONT_WALL_CONFIRM_N 3`) 필수** +
  open/asym-open/asym = `34/34/14cm`, wide = `34/34/12cm`, side-only(front 상실 시) open `36`, 확인 2회
- 벽 판정 후 좌우 결정: `FRONT_DECIDE_CM 20` fresh 연속 2프레임, `BRAKE_CREEP_PCT 32`, 캡 700ms
- 직진 조향: `CENTER_HDG_ERR_LPF_ALPHA 0.25`, `CENTER_HDG_KP 0.50`, deadband `0.6°`,
  lateral cap `8°`, 측면 불감대 `3cm`(내부 스케일 0), lateral KD 일반/전방개방 `0.030/0.025`,
  yaw 댐퍼 `0.28/8dps/6%`, steer cap `18%`
- 직진 최고속도: 기본/직선고속/좁은/넓은 = `63/68/63/69`, min `36`. 코너/상태전이 뒤 settle은
  `650ms`, 일반/중간/넓은 `48/50/52`; yaw `12→45dps`에서 속도 상한을 최고속도→`38`로 낮춤.
- SIDE_AVOID: `48/-32` 피벗은 `18°`까지만, 이후 `36/36` 전진으로 측면 8cm×3 확인까지 이탈.
- SPIN 안전복귀: 전방 danger fresh 3회 또는 `F<34 && side<6` 2회면 open-latch를 무시하고 후진.

---

## 4. 빌드·실행·검증

```
make -j8                # Debug → build/PR_CAR.hex
make RELEASE=1 -j8      # Release → build/release/PR_CAR.hex  ← 주행용
```
- 툴체인: arm-none-eabi-gcc 14.3 (STM32CubeIDE 2.1.0 번들, PATH 등록됨). 호스트 테스트 불가 →
  **유일한 회귀 테스트 = 실차 + 영상 교차분석(§6)**.
- 모터 벤치: `MOTOR_TEST 1` → 전진/제동/피벗/후진 시퀀스.
- 텔레메트리: `"T,<t_ms>,<f cm>,<L mm>,<R mm>,<h×10>,<vL>,<vR>,<st>,<fl>,<steer>\n"` @10Hz.
  `fl` 비트: b0 f_valid, b1 side_valid, b2 imu_live, b3 power, b4 mode.
  RX: `1/0/A/M/U/D/L/R/S` + `#KEY=VAL`. **부팅 시 모터 OFF — `A` 수신으로 시작.**
- 필드 추가는 프레임 끝 append만 (기존 인덱스 = 웹앱 계약). DS_ enum 값 변경 금지.

### 실주행 체크리스트 (§5.13 빌드용)
1. `build/release/PR_CAR.hex` 플래시, 주행영상 + 웹앱 화면녹화를 거의 동시에 시작.
2. 첫 코너 탈출 뒤 0.65s: CRUISE가 바로 최고속도로 튀지 않고 코스축 ±10° 안으로 가라앉는지 확인.
3. SIDE_AVOID 전후 heading 변화가 약 20~25° 이내인지 확인. 30~40°면 `18°→36/36` 전환이 늦은 것.
4. SPIN에서 F<12가 3 full frame 지속되거나 `F<34 && side<6`가 2회면 REVERSE로 빠지는지 확인.
5. 안정된 직선에서는 최고속도 `63/68/63/69`가 그대로 나오는지, 치우침·yaw 때만 감속하는지 확인.
6. 방지턱 구간 스톨은 우측 엔코더 0 하드웨어 문제와 분리해서 기록. 무충돌 완주 전 추가 속도 상향 금지.

---

## 5. 최신 수정 요지

### 5.27 미완성 턴 exit = 강개방 대각선 오독 + 스냅턴/펀치아웃/멈칫 완화 (IMG_3074 ↔ mapp_v5, 오프셋 ≈0) — **최신, 빌드 검증·실차 미검증**

곡선 구간 정상 통과 확인. 남은 상습 충돌(주행 20~24s) 판독:
19.5s SPIN(우) → **20.0s 진행각 +40°에서 exit** (Δ+143, 코스축 180 — 37° 미완성) →
대각선 순항 spd58 → 21.0s L=45mm 외벽 충돌 → 2차 피벗 +48° → 축 도달. 23.5s 다음
코너 동일 패턴(Δ+130 exit → R38mm). **원인 = §5.24 강개방(F≥52) 조기 exit이 축근접
조건 없음**: 코너 포켓의 대각선 개구도 "열림"으로 읽혀 턴을 반만 하고 나감. 나간 뒤엔
§5.19 프리휠(오차>25° 억제)이 잔여 재정렬까지 차단 → 벽에서야 2차 턴.

수정 4종:
- **강개방 exit 축근접 게이트** `TURN_EXIT_ALIGN_LOOSE_DEG 25`: F≥CLEAR exit은
  |축오차|≤25°일 때만. 와이드존(양측≥30cm) exit은 별도 경로로 불변(광장 45° 대응 유지).
  25° 이내 잔차는 프리휠 문턱(25°)과 정합 — exit 직후 축유지가 즉시 잡는다.
- **스냅턴**: TURN_SPEED 56→62, TURN_INNER 34→36, PID_MIN 44→48, FINE_MIN 40→44.
- **펀치아웃**: SETTLE 650→450ms / 48→52%.
- **직진 멈칫 완화**: 거버너 감속 개입 지연 — HDG 14→18°(min 40→44%),
  YAW 45→60dps(min 38→44%); FRONT_STOP 34→30 (코너 접근 F=36 호버링이 만들던
  경계선 브레이크 컷; DECIDE 16 < STOP 30 사다리 유지).

### 5.26 사선벽 노즈램 = 전방 초음파 스펙큘러 무에코 (IMG_3069 ↔ mapp_v4, 오프셋 ≈+4.8s) — **이전 빌드**

충돌 3회 판독 (주행 13~14s/18~19s/24~25s ↔ 모니터 18s/23s/29~30s):
**박는 순간 F=80("열림")·L=31mm·duty 높음·spd 3~4cm/s** — 사선(斜線) 벽은 초음파를
반사각으로 흘려보내 무에코 = timeout = open 판정. 전방이 원리적으로 못 보는 각도라
speed governor가 60%+ 유지한 채 노즈램. 측면 ToF(90°)는 벽이 옆에 온 뒤(3cm)에야 감지.

수정 2종 (둘 다 "박을 것 같으면/박으면 피하는 턴" 경로):
- **고속 예방 측면 회피** `SIDE_FAST_DUTY_PCT 52 / SIDE_AVOID_FAST_CM 9`:
  듀티 평균 ≥52%면 side_emergency 문턱 5→9cm — 사선 접근에서 측면이 유일한 사전 신호,
  빠를수록 일찍 발화. 저속(거버너가 이미 감속한 상태)은 기존 5cm 유지.
- **크루즈 램/스톨 백스톱** `CRUISE_STALL_*` (42% / 6cm/s / 350ms / grace 400ms):
  듀티 ≥42% 유지 + 좌측 엔코더 속도 <6cm/s가 350ms 지속 = 전방 판독 무관 물리 접촉 →
  직후진(back_enter)→브레이크→판정턴. dbg.v_l 사용(MotorTask 동일 문맥, 경쟁 없음;
  우측 엔코더는 하드웨어 0 고정이라 미사용). grace 400ms가 기동/상태 진입 가속 오탐 차단.
  부수효과: 방지턱 스톨(§8-3)도 이제 후진→재시도로 탈출 (기존: 무한 정체).

### 5.25 턴 완료 = 헤딩 각도 변화가 유일 판정 (사용자 명세, IMG_3068 ↔ mapp_v3) — **이전 빌드**

사용자 관찰 확정: 턴 중 "쓸데없는 동작"(후진 끼어듦, 과한 판단) + 과회전 벽박기/역주행.
판독으로 원인 2개 특정:
1. **blocked-nose 백아웃**: 피벗 중 F<12 (또는 F<34+측면<5cm) 2프레임 → REVERSE. 그러나
   37~45cm 복도에서 27cm 차가 피벗하면 노즈가 벽 5~12cm까지 스치는 게 기하학적 정상 —
   건강한 회전을 후진 청크로 끊는 주범 (mapp_v3 28s 실측). **삭제** (block_n·
   SPIN_BLOCK_CONFIRM_N 제거). 진짜 낌은 회전 동결 → 웨지 체크(400ms)가 잡음.
2. **완료 판정이 전방/축정렬에 인질**: 진행각 88° 도달해도 "전방 open + 축정렬" 미충족이면
   exit 불가 → 바닥듀티(40%)로 계속 회전 → 과회전. **NEW: progress ≥ TURN_TARGET(88°)
   + course 게이트면 무조건 CRUISE 복귀** — 그 시점 전방이 가까우면 크루즈가 정지 자세로
   브레이크→판정 (계층 책임 분리). 조기 exit 3종(축정렬/와이드/강개방 F≥52)은 유지.

사용자 변경 병기: FRONT_DECIDE_CM 17→16 (직접 수정분 유지).
남은 회전 중단 경로 = 웨지(400ms 동결)·SPIN_MAX(1600ms)·§5.22 와이드 과회전 예산뿐.

### 5.24 턴 = front-primary로 재편 (사용자 명세 갱신: decide 16~17cm, 측면 단독 턴 금지) — **이전 빌드**

사용자 관찰: "전방 안 보고 좌우만 보고 계속 턴하는 느낌" + 역주행 잔존. 명세 3항:
① 턴 시작은 전방 ~16~17cm에서만, 그 시점 좌우 비교로 방향(방향 판정 자체는 정확함)
② 회전 중 전방이 크게 열리면 즉시 종료 (과회전 금지)
③ 측면은 데드존(초근접)이 아니면 무시하고 직진 유지.

수정:
- **DS_CORNER 아크 전면 삭제** (corner 검출/corner_enter/corner_run/command_arc,
  ARC_*·SIDE_OPEN/ASYM/NEAR_SAFE·CORNER_* 노브, FRONT_ARC 사다리 칸): 전방 44cm+측면
  비대칭으로 발화하던 유일한 "측면 주도 턴" 경로 = 역주행 잔존의 진원. STOP(34) 확정이
  항상 선행하므로 시퀀스는 **cruise → BRAKE(34) → 크립 → decide(17, fresh+stable) →
  SPIN 피벗**뿐. DS_CORNER enum은 웹앱 계약이라 유지(미사용 상태).
- **FRONT_DECIDE_CM 20→17** (§5.9 개정), BRAKE_CREEP_MAX 700→900ms (17cm까지 크립 예산).
- **강개방 조기 exit**: SPIN exit 정렬 조건에 `front_open_at(FRONT_CLEAR 52) &&
  wide_exit_course_ok(60°)` 추가 — 코가 확실히 열리면 축정렬 전이라도 종료(과회전 방지).
  §5.22 course 고삐(60°)는 모든 비축 exit에 공통 적용 유지.

트레이드오프 (실차 관찰 항목): 광장 45° 각면을 초음파가 사선 입사로 못 볼 수 있음 —
아크가 하던 조기 회피가 사라져 DANGER(12cm)/STOP 의존. 각면 충돌 잦으면 §5.19
프리휠·repel로 부족한 것이므로 그때 전용 대응 설계 (아크 부활 금지, front-primary 유지).

### 5.23 간헐 역주행 = 가짜 벽 판정턴 2연쇄의 코스축 회전 (IMG_3065 ↔ mapp_v2) — **이전 빌드**

mapp_v2 판독: 순항 79~96cm/s(§5.19 효과), 완주급 주행이나 검정테이프(방지턱) 구간에서
F=3/9/10/16 적색 폭풍 → BRAKE/판정턴 연쇄, Δ±160° 배회. **간헐 역주행의 정체**: 가짜 벽
판정턴이 나올 때마다 exit에서 course_heading이 90°씩 재스냅 — **2연쇄면 코스 프레임이
180° 회전해 모든 게이트(축정렬·dev<115)가 역주행을 합법 통과**시킨다. "될 때 안 될 때" =
가짜 브레이크 2연속 발생 여부의 확률 문제.

수정 3종:
- **전방 확정 안정성 게이트** `FRONT_STABLE_CM 4` (drive.c): 정면 실벽은 프레임당 수 cm씩
  연속 접근, 바닥/턱 에코는 순간이동(3→30→16). front_wall_n(코너)·front_stop_n(브레이크)·
  decide_n(방향판정) 카운터는 직전 표본과 ±4cm 이내로 일치하는 연속 표본만 센다.
  DANGER(12cm) 사다리는 무게이트 유지 (안전 우선). 잔존 구멍: BRAKE 크립 타임아웃(780ms)
  폴백은 안정성 우회 — 지속 바닥에코는 여전히 방향판정까지 갈 수 있음(피치 게이트가 보완).
- **방지턱 피치 게이트** `FRONT_PITCH_MAX_DEG 6.0` (freertos.c, §8-3의 [추정] 항목 실행):
  |pitch|>6°인 프레임의 전방 에코 무효. IMU 사망 시 자동 비활성. 전방이 자주 먹통이면
  dbg.pitch 평지 오프셋 확인 후 문턱 상향.
- **속도 +5%** (§8-2 규칙 준수): SPEED_TOP 60→63, SPEED_SETTLE 46→48. 안전 플로어
  (FRONT_MIN/SIDE_MIN 등 제동예산 캡)는 불변.

### 5.22 역주행 = 와이드존 과회전 커밋 + 단일벽 고속 수렴 접촉 (IMG_3055 ↔ mapp_v1) — **이전 빌드**

mapp_v1 판독: §5.18~5.21 유령 초근접은 해소 확인(개활측 L/R=1000 캡 정상). 남은 두 실패:

1. **역주행 (32~36s, 주행 33~36s)**: SPIN이 Δ-20 → Δ+134, **154° 과회전** 후 CRUISE 진출,
   역방향으로 spd 54 순항. 사슬: ① exit의 전방 open(44) 요구가 정위치에서 가짜 F16(바닥에코)에
   막힘 → 회전 지속 ② 목표 88° 초과 후에도 PID 바닥듀티(40%)로 무한 회전 (멈춤 상태 없음)
   ③ §5.19 와이드존 exit이 축정렬 없이 통과 + course 게이트 115°는 154°를 못 막음
   ④ §5.19 프리휠이 역방향 순항을 안정화. **§5.19의 부작용 조합.**
2. **턴 후 직진 벽 접촉 (38~40s)**: 와이드 복도 단일벽 추종에서 벽 22cm는 감속 캡(12cm) 밖 →
   spd 54~60 유지한 채 완만 수렴 → R 221→24→1mm. 반발존(12cm)이 속도를 못 따라감.

수정 (§7-17/-20 준수, drive.c+drive_config.h):
- **와이드존 과회전 예산** `TURN_WIDE_OVER_DEG 110`: sides_open_wide에서 끊김 없는 sweep
  (turn_leg)이 110° 초과 && exit 미성립 → back_enter(3점턴 후 재접근). 유령 전방을 쫓아
  역방향까지 갈리는 것 차단. 근접벽 포켓은 무제한 sweep 유지 (§5.16 180° 반전 경로 보존).
- **와이드존 exit 전용 course 고삐** `WIDE_EXIT_COURSE_DEV_DEG 60`: 축정렬 없이 나가는
  exit(SPIN·CORNER)은 course_dev < 60°일 때만 허용 (turn_leg≥180 반전 예외 유지).
  축정렬 exit은 기존 115° 게이트 그대로.
- **단일벽 속도 캡** `SPEED_SINGLE_SLOW_CM 25 / SPEED_SINGLE_MIN_PCT 42`: 단일벽 추종
  중 벽 ≤25cm부터 SOFT(12cm)까지 42%로 램프 감속 — 반대편 무참조 상태의 수렴 예산 확보.
  pair-valid 복도 최고속은 불변.

### 5.21 "옆에 아무것도 없음"의 1급 처리 — 초근접 신뢰 게이트 (IMG_3053 ↔ 20260715-1149) — **이전 빌드**

mappv-1149 판독: §5.20 이후에도 광장에서 R=7~22mm 가짜 초근접 잔존 (0s R22, 14s R7).
"센싱 반대" 메커니즘 확정: **가짜 R 2cm + 실제 L 22cm → pair 합 24cm ≤ 52 → pair-valid
성립 → 센터링이 유령벽 반대쪽(=진짜 벽)으로 조향** + decide_turn_direction도 l≫r로 진짜 벽
쪽 회전 고착. 유령 한 값이 센터링/방향판정/side_emergency/SPIN pinch 네 소비자를 동시 오염.

물리: VL53L0X 실효 사거리 ~1.2m(20ms 버짓·판지 사면은 그 이하). 사거리 밖 = 측정 불능인데
칩이 status=valid인 0~40mm 쓰레기를 간헐 반환 — status 필터로는 못 거른다.

수정 (freertos.c 센서 컨디셔닝 계층, drive.c 무변경): **초근접 신뢰 게이트**.
mm < 60(TOF_NEAR_TRUST_MM)인 표본은 둘 중 하나일 때만 신뢰 —
(a) 접근 연속: 직전 median < 12cm (실벽 접근은 반드시 중간 거리를 거침),
(b) 안정 연속: 연속 표본 지터 ≤12mm × 3회 (정지 실벽은 ±수 mm, 쓰레기는 0↔37mm 플리커).
어느 쪽도 아니면 `TOF_CAP_MM`(개활)으로 공표. 실벽 페널티: 고립 등장 시 인정까지 ~60ms.

한계·후속: (1) 3연속 안정 쓰레기가 우연히 나오면 잠깐 통과 — median+§5.9 확인계수가 흡수,
(2) status 코드는 `dbg.tof_st_l/r`(SWD)로 확인 가능 — **11 고정 + 초근접 상수면 커버(테이프)
크로스토크 = 하드웨어**, 애퍼처 앞 테이프 제거가 근본 (§5.20 판정 유지),
(3) 1149 영상이 §5.20 빌드였는지 미확인 — 이번 빌드는 §5.20+5.21 모두 포함.

### 5.20 개활 측면 잔존 쓰레기값 = sigma-fail 통과 구멍 (정지 포즈 사진 ↔ 20260715-1127 클립) — **이전 빌드**

정지 재현 (MANUAL, 광장 좌벽 ~22cm/우측 개활): L=218mm 안정(실측 일치), **R=0→37→20→32mm
플리커** — §5.18 필터를 통과한 잔존. phase/signal fail은 8190으로 걸렀지만 **sigma fail(7)을
"정확도 저하일 뿐"으로 통과**시킨 게 구멍: 20ms 버짓 + 타깃 없음/원거리 사면에서 sigma fail이
0~40mm 쓰레기 mm를 달고 나온다. 근접 실벽은 신호가 강해 sigma fail이 안 뜨므로 reject 안전.

수정: PollRangeMM status 정규화를 **11(valid)만 통과**로 강화 (8/10→30mm 유지, 나머지 전부
8190). + `VL53L0X.last_code` / `dbg.tof_st_l/r` 추가 — SWD Live Expressions로 문제 포즈에서
status 코드 직접 확인 가능 (11이 찍히면서 여전히 <50mm면 하드웨어 크로스토크 확정).

하드웨어 각도 질문 판정: **90° 유지가 정답**. 이 현상은 각도 문제가 아니라 "타깃 없음" 처리
문제 — 70°로 꺾어도 개활 방향에 타깃이 없으면 동일하게 재발하고, 대신 l/r이 순수 횡거리가
아니게 되어 §5.17 중앙유지/판정 기하 전부와 v1의 45° 오염 교훈(§5.10~5.12)이 되돌아온다.
점검 우선순위: ① 우측 ToF 애퍼처 위 투명테이프/오염 제거(커버 크로스토크 = valid status로
초근접 상수 반환, 코드로 구분 불가) ② 전방 초음파 저장착 재장착(F=16 고정 바닥에코, §8-4).

### 5.19 곡선(광장) = 90° 격자 부재 구간 + 직진 복귀 저권한 (IMG_3051 ↔ mappv14, 오프셋 ≈0s) — **이전 빌드**

mappv14 판독: §5.18 wraparound 필터 동작 확인 (개활측 L/R=1000mm 캡 정상 표기, 가짜 초근접 소멸).
남은 두 실패:

1. **복도 6~13s**: 좌벽 7.8cm 붙은 채 저속 크롤 → 가짜 전방(F24/F9, 벽 스침 에코/방지턱 피치
   추정) → BRAKE→REVERSE→SPIN 순환 후 14s 복귀. 근본 노출원인 = **복귀 권한 부족**: lateral
   11° 캡·steer 18% 캡·KP 0.5로는 붙은 벽에서 탈출이 수 초 걸려 스침 에코에 계속 노출.
2. **광장 16~36s**: CORNER는 발화했으나 88° 고정 목표 + exit 축정렬(±10°) 게이트가 45° 각면
   팔각 기하와 충돌 — 회전이 90° 배수 포즈까지 강제되고, 그 포즈는 다음 각면과 충돌 → 18초
   BRAKE/SPIN/REVERSE 순환. 크루즈 heading-only 모드도 축오차 -95° 등을 30% steer로
   피벗-파이팅해 광장에서 스스로 헤딩 스핀 유발.

수정 (§7-17/-20 준수 — heading_ref는 여전히 90° 축만, course_dev 게이트 유지):

- **와이드존 회전 exit** (`sides_open_wide()`: 양측 ≥30cm 또는 invalid): SPIN·CORNER의
  30° 조기 exit에서 axis_aligned 요구를 해제 (`|| sides_open_wide`). 복도(한쪽 <30cm)에서는
  기존 게이트 그대로. 팔각 광장 = 30~50° 단위 짧은 회전 연쇄로 곡선 추종.
- **오프축 프리휠** (`CENTER_OPEN_FREE_DEG` 25): heading-only 모드(정의상 양측 ≥30cm)와
  single-track(반대편 ≥30cm)에서 |축오차|>25°면 축유지 P항 차단 — yaw 댐핑만으로 직진 유지,
  벽추종(single)은 벽만 본다. pair-valid 복도·근접벽 구간은 풀 축유지 유지 (§5.10 회귀 없음).
- **직진 복귀 권한 상향** (요청: 쏠림 시 빠른 스냅 복귀): lateral KP 0.32→0.45·KD
  0.030→0.045·knee 0.8→1.0·캡 11→15°(12° graze 밴드는 45° 장착과 함께 사망 — §9 해당
  불변식 폐기)·slew 70→110, hdg KP 0.50→0.80·KP2 0.60→0.85, steer 캡 18→26%·align
  36%·slew 140→240%/s. 발산 방지 짝: yaw KD 0.28→0.34·댐프캡 6→9%, repel KP 1.35→1.9,
  SIDE_SOFT 10→12cm. LPF/불감대 불변 (§5.8 준수).

검증: `make -j8` 클린. 실차 체크: (1) 직선 위빙 재발 여부 — 발산 시 hdg KP·steer slew부터
반값 (2) 벽 7cm 붙임→중앙 복귀 시간 (3) 광장에서 CORNER/SPIN이 30~50°로 끊어 도는지,
BRAKE 순환 소멸 여부 (4) 복도 회전 exit이 여전히 축정렬로 나가는지.

### 5.18 곡선 광장에서 측면 ToF "반전" 판독 = VL53L0X wraparound (IMG_3050 ↔ mappv13) — **이전 빌드**

증상: 주행영상 ~12s(모니터 ~13.5s, 오프셋 +1.6s) 곡선 광장 진입 후 12초간 SPIN/REVERSE
스래시, 사용자 구출. 모니터 판독: **우측 ToF가 18~58mm(초근접)를 지속 보고하는데 실제
영상에서는 우측이 광장 개활 방향** — L556/R58, L659/R49, L484/R33. 열린 쪽이 초근접 벽으로
"반대로" 읽힘. 복도로 나오면 정상 (양쪽 벽이 사거리 안).

원인: VL53L0X는 타깃이 언앰비규어스 사거리(~1.2m, 20ms 버짓에서 더 짧음) 밖이면 8190이
아니라 **wraparound된 가짜 초근접 mm를 반환**하고, 그 사실은 RESULT_RANGE_STATUS(0x14)의
phase/signal fail 코드로만 표시된다. `VL53L0X_PollRangeMM()`이 status를 안 읽고 mm만
통과시켰음 (Pololu 계열 경량화의 구멍). 지속 오염이라 median-3도 못 거름.

연쇄: 가짜 R<5cm → ① `SIDE_NEAR_SAFE_CM`(6) 게이트가 좌코너 아크 차단 = "곡선 턴이 안 됨"
② side_emergency(5cm)/repel 오발 ③ decide_turn_direction 항상 좌회전(l≫r) → 커브 기하와
충돌 → SPIN exit 게이트(축정렬) 도달 불가 → SPIN_MAX→REVERSE→SPIN 순환.

수정 (드라이버 소유 노브, vl53l0x.c/.h): PollRangeMM이 status bits[6:3] 판독 후 정규화 —
11(valid)·7(sigma fail, 실측이며 noise는 median/LPF 몫) 통과, 8/10(min-range clip) →
30mm 고정, **그 외(phase/signal/HW fail) → 8190 강제** → 기존 캡 경로가 open으로 처리.
freertos.c/drive.c 무변경. 대시보드에서는 해당 구간 L/R이 1000mm(cap)으로 보이면 정상.

전방 F 3~16cm 적색 플랩은 별건: 피벗 중 빔 스윕 + 저장착 바닥에코(§8 하드웨어 재장착 건).

### 5.17 드라이브 코어 v2 전면 재작성 (mappv12 판독 + 90° ToF 전환) — **이전 빌드**

mappv12(§5.16 빌드, 50.8s) 판독: 완주는 하나 상태 전이가 0.5s 시트 102칸 중 ~60칸,
F<12cm 이벤트 12회+, Δ ±80° 스윙 반복 — **복구 로직이 완주시키는 것이지 주 제어가 설계
범위에서 돌지 않음.** §5.13→5.16 예외 웹(노브 100+, 카운터 40+)이 서로의 전제를 깨는 단계
진입 → 사용자 합의 하에 코어 재작성 (인프라·교훈은 유지).

**전제 하드웨어 변경: 측면 ToF 90°(전방 초음파와 직각) 재장착.** v1 예외 다수가 45° 장착의
전방 기하 오염 방어였음 — 90° 전환으로 존재 이유 소멸.

구조 (drive.c 단일 파일, drive_control.c/.h 삭제, Makefile·CMakeLists 양쪽 반영):

| 계층 | 상태(enum 불변) | 내용 |
|---|---|---|
| CRUISE | DS_CRUISE | 센터링 캐스케이드(§5.8 LPF/불감대 + §5.15 knee) + **속도 거버너(1급): 전방거리 캡이 제동·판정 예산 보장, 상한 60%** |
| TURN | DS_CORNER(아크 주) / DS_SPIN(피벗 폴백) | 코너 = 확정 전방벽(44cm fresh×3) + 측면 비대칭(30/+12) ×2 확정(+dropout 1). 아크가 막히면 진행각 보존한 채 피벗 에스컬레이션. 피벗 exit = 누적≥30 + 축정렬≤10 + 전방open + **코스반전 게이트(모든 exit 공통, §7-20)** |
| RECOVER | DS_BRAKE / DS_REVERSE / DS_SIDE_AVOID / DS_HOLD | BRAKE=§5.9 검증+20cm 크립+판정(불변). 후진: 피벗발=3점턴 청크→resume(§5.14), 예산 RECOVER_RETRY_MAX 3/에피소드 초과 시 직후진+재판정으로 강등(§5.15·5.16 데드락/역주행 방지 통합) |

유지된 교훈: wrap180 증분 누적 + turn_leg 분리(§5.16), fresh 연속확정(§5.9), 90° 축 스냅만
(§5.10), 스키드 돌파 듀티 가드, IMU 백스톱(SPIN_BLIND/LAUNCH), SIDE_AVOID 18° 제한(§5.13),
steer_mode 텔레메트리 값(0/3/4/5) 및 DS_ enum 웹앱 계약.

삭제된 것: 45° 그리드 전부, graze 삼각법 모델, side-only 코너, 대칭코너 course 규칙(판정
폴백으로만 축소), HREF 블렌드/axis-resnap, corridor wide/narrow 프로파일 분기, swap-evidence,
wedge-refill(retry 예산으로 통합), spin_open_committed. 노브 ~160→~90, 실튜닝 표면 ~15개.

**측면 기하 상수는 도면 유도값 [재실측 필수]**: SIDE_PAIR_MAX 52 / OPEN 30 / ASYM 12 /
NEAR_SAFE 6 / SOFT 10 / HARD 7 / AVOID 5 / SINGLE_TARGET 13. 수동주행 1회로 §1 실측표를
90° 기준으로 재작성한 뒤 조정할 것. 전방 사다리(12/20/34/44/52/68)는 초음파 불변이라 유지.

검증: `make -j8` / `make RELEASE=1 -j8` 클린(경고 0), text 52.9→44.0KB. 실차 확인 순서:
(1) 직선 위빙 여부(§5.8 회귀 체크) → (2) 코너가 CORNER 칩으로 도는지 → (3) F<12 이벤트
빈도(거버너 효과) → (4) 완주. 문제 시 §6 워크플로 그대로.

### 5.16 역주행 = 3점턴 누적 진행각이 코스반전 예외를 unlock (IMG_3047 ↔ mappv11) — **이전 빌드(v1 코어)**

영상 메타데이터: 주행 18.478s(23fps), 웹앱 29.033s(30fps). §5.15 빌드 실차: 속도·코너는
빨라졌으나(첫/둘째 코너 각 ~1s) **9.5s에 코스 정반대로 진출해 역주행, 완주 실패, 26s 사용자
MANUAL 개입**.

역주행 타임라인:

| 웹앱 시각 | 텔레메트리 | 판독 |
|---:|---|---|
| 5.5→7.0s | CRUISE `L65~82mm` 좌벽 허깅 → 곡선 우코너 진입 시 `L409 R67` | 우벽 6.7cm 허깅 — 로컬 기하가 "좌측 열림"으로 뒤집힘 |
| 6.5→7.5s | SPIN 좌회전, `Δ+32→+4.8`에서 CRUISE 진출(F53) | 코스는 우코너인데 좌로 판단 — 로컬 기하만으론 구분 불가 |
| 7.5→9.0s | steer `+35~39` + 속도 음수 반복, `Δ+4.8→-45→-94→-138` | **3점턴 후진 체인이 좌회전 유지한 채 회전 지속, 진행각 계속 누적** |
| 9.5→10.0s | CRUISE `Δ+161→+177`, F41, steer 0 | 에피소드 누적 +188° ≥180 → `forward_ok`의 "막힌 포켓 = 역방향 허용" 예외 발화 → **Δ180에서 정상 진출 = 역주행** |
| 10.5→18s | REVERSE/SPIN/BRAKE 뒤엉킴, `F6` 웨징 | 복구 시도했으나 실패, 이후 MANUAL |

근본원인: §5.14의 `spin_resume` 진행각 보존은 exit 게이트 재회전 방지가 목적이었는데,
**코스반전 예외(진행각 ≥180)의 전제 — "한 번의 끊김 없는 180° 회전 = 진짜 막힌 포켓" — 를
함께 무너뜨렸다.** 후진 청크로 이어붙인 누적은 일반 코너 싸움에서도 180을 넘는다.

수정:

1. **`turn_leg_deg` 분리**: 총 누적(`turn_accum_deg`)은 exit 게이트용으로 유지(§5.14 목적 보존),
   `turn_leg_deg`는 `spin_resume`마다 0으로 — **코스반전 예외 3개소(SPIN_MAX 분기·forward_ok·
   축기하 exit)는 leg만 본다.** = §5.14 이전의 강한 전제 복원.
2. **축기하 exit(`spin_axis_exit_ready`)에 코스 게이트 추가**: 기존엔 course_dev 무검사로
   진출 가능한 역주행 누수 경로였다.
3. **웨징 리필 캡** `SPIN_WEDGE_REFILL_MAX 2`/에피소드: 무한 리필은 후진 체인 무한 회전 허용.
   spin_enter/spin_restart에서 리셋.

검증: `make -j8`, `make RELEASE=1 -j8`, `git diff --check` 통과. 실차 확인 포인트:
(a) 곡선 우코너에서 좌스핀이 Δ180 근처에서 **막히고**(forward_ok) SPIN_MAX → 플립 → 우회전으로
되감는지, (b) 완주 여부, (c) §5.15 확인 포인트 3종 유지.

### 5.15 웨징 데드락 + 복귀 게인 부족 + 아크 미발화 (IMG_3046 ↔ mappv10) — **이전 빌드**

영상 메타데이터: 주행 28.932s(29fps), 웹앱 36.967s(30fps). §5.14 빌드의 첫 실차 결과로 추정 —
코너 자체는 1s 내 완료(1.0→2.0s 우, 4.5→5.5s 좌)로 §5.14 효과 확인. 새 문제 3건:

| 웹앱 시각 | 텔레메트리 | 판독 |
|---:|---|---|
| 7.0→7.5s | CRUISE `L83→57mm`, steer `-11→-18` | 좌벽 허깅 — lateral 게인(0.32×~8°cap)으로는 복귀 ~4cm/s |
| 8.0s | `Δ+29.1`, `L222 R219` | repel/guard가 한번에 우측으로 +33° 리바운드 (사용자 관측 "우측 과다") |
| 8.0→9.0s | steer `18→15`, `Δ+29→+15` | 복귀 크롤: KP 0.5×29°=14.5%뿐 — **클램프가 아니라 게인이 바닥**. 결국 우벽 `R43mm` SIDE_AVOID |
| 15.0→16.0s | BRAKE `F4` → SPIN | 코너 포켓에 정면 박음, reverse 예산 2회 소진 |
| 16→29.5s | **SPIN↔BRAKE 교대, `Δ+114~116` 동결, `F=10`, `L≈4cm`, 속도 0** | **13s 데드락**: 회전 물리적 불가 + reverse_count 리셋 경로가 cruise/corner 진입뿐 → 후진 영영 금지 (사용자 관측 "마지막 멈춤") |
| 30→34s | SPIN steer `-41↔+43` 교대, `Δ` ±15° 왕복 | 웨징 해제 후 `TURN_WRONG` 플립이 스왑 예산 무시 → 미러 반턴 핑퐁 |
| 1.0s, 4.0s | SPIN 정지피벗 (아크 아님) | 1.0s: 판정측 `R 34~36cm`이 34 경계에서 플랩 → confirm 리셋. 4.0s: 우벽 허깅(`R6.6`)으로 near_min 7 미달 (사용자 관측 "멈췄다 턴") |

근본원인과 수정:

1. **웨징 데드락**: ROT_STUCK(400ms 내 8° 미만) + `front_recent_below(34)`이면 reverse 예산
   리필 후 3점턴 후진. 예산의 목적(개활지 미러 후진 핑퐁 방지)은 유지 — 회전 불가+벽 확인은
   후진 외에 수가 없는 상태라 리필이 맞다.
2. **TURN_WRONG 플립도 스왑 예산 준수**: `spin_restart(in, swap_count < SWAP_LIMIT)`.
   블록된 피벗의 스크럽 리바운드(-20° 왕복)가 예산 소진 후에도 무한 플립하던 것 차단.
3. **헤딩 knee 게인** `CENTER_HDG_KNEE_DEG 10 / KP2 0.6`: 29° 오차 시 steer 14.5→26%.
   `CENTER_STEER_MAX_ALIGN_PCT 30`(§5.14)은 이제 실제로 쓰인다. 위빙 레짐(<10°)은 기존 게인 그대로.
4. **횡오차 knee** `CENTER_LATERAL_KNEE_CM 10 / GAIN 0.8` + lateral cap `8→11°`
   (graze 게이트 12° 아래 유지): 20cm+ 치우침 복귀 ~40% 가속 — 벽 허깅 자체를 줄여
   리바운드(문제 2번 관측)와 near_min 미달 아크 불발(문제 3번)을 근원에서 줄인다.
5. **코너 confirm 1프레임 dropout 허용** `CORNER_DROPOUT_TOL_N 1`: front_wall 유지 중이면
   경계 노이즈 1프레임에 candidate/count 유지 + pre-confirm 아크 지속. 실측 코너 33~50cm이
   34cm 문턱에 걸치는 건 §1 실측표 그대로고, **문턱 자체는 낮추지 않았다** (§7-18 준수).

검증: `make -j8`, `make RELEASE=1 -j8`, `git diff --check` 통과. 실차 확인 포인트:
(a) 포켓에서 SPIN↔BRAKE 무한교대가 사라지고 후진이 다시 나오는지,
(b) 직선 치우침 복귀가 1s 내인지 (Δ±30 스윙 소멸),
(c) 첫/둘째 코너가 CORNER 칩(롤링 아크)으로 뜨는 빈도.

### 5.14 코너 포켓 스래시 = 3점턴 부재 (IMG_3045 ↔ mappv9) — **이전 빌드**

영상 메타데이터: 주행 37.957s(23fps, 480×854), 웹앱 44.867s(30fps, 572×702). 오프셋 ≈ 0s
(주행 12s 코너 ↔ 웹앱 11.75~15.5s SPIN 클러스터 일치).

사용자 관측 "12초쯤 미세 턴턴턴 + 헤딩 360↔0 튐"의 실체 (0.25s 판독):

| 웹앱 시각 | 텔레메트리 | 판독 |
|---:|---|---|
| 11.75→12.0s | BRAKE `F23` → SPIN `F19`, `L18/R15cm`, steer `-37` | 정상 진입: 20cm 결정선에서 우피벗(정방향) 시작 |
| 12.25s | SPIN steer `+38`로 반전, `L14.5/R19.3` | **단발 측면 스파이크가 방향 플립** — 반대로 돌다 노즈가 벽으로 |
| 12.5→14.5s | REVERSE `F7` → SPIN → BRAKE → SPIN, `F=7~10` 고정 | **직후진 220ms → 크립 재접근 → 같은 포즈 복원** 사이클 2회. 회전 ~25°/s로 웨징 |
| 15.5s | CRUISE `Δ-173.9` | 코너 하나에 3.75s 소요 후 탈출 |
| 15.5→17.25s | CRUISE `Δ-174→-179→+179` (RAW 359↔0) | **±180 통과는 표시만 점프** — 제어는 전부 wrap180 차분, 조향 steer 1~8로 평온 |
| 30.5→32.0s | CRUISE `Δ+112→+93`, steer +10~18, 이후 `좌59mm` SIDE_AVOID | 코너 abort 후 축에서 22° 벗어난 채 고속 재진입, 18% 클램프로는 대각 주행이 벽까지 |
| 33.5→39.5s | SPIN/REVERSE/BRAKE 교대, `F=5~16` | 위 대각 진입이 만든 두 번째 포켓 스래시, 6s 소요 |

**"90° 턴 완료 시 헤딩 초기화" 제안은 기각**: 모든 각 연산이 `drive_wrap180` 차분이라 ±180/0↔360
경계는 산술적으로 무해하고(§7-10과 동일 원리), course_zero를 리셋하면 `symmetric_course_direction`
의 최종 코너 규칙과 대시보드 Δ 연속성이 깨진다. 근본원인은 wrap이 아니라 아래 3개.

근본원인과 수정:

1. **스핀 방향 스왑이 단발 판독으로 발화**: 포켓 회전 중 측면 빔이 코너 모서리를 스치면 한 프레임
   `selected<8 && other>selected+6`이 성립. → `SPIN_SWAP_CONFIRM_N 3` 연속확정 도입
   (`swap_evidence_count`, §5.9 fresh-연속확정 원칙을 스왑에도 적용).
2. **SPIN발 REVERSE가 직후진**: 후진으로 번 8~10cm를 BRAKE→크립이 그대로 반납하고 진행각도 0으로
   리셋 → 88° 목표 재양자화. → **3점턴**: `spin_reverse_enter`가 회전방향을 유지한 채
   `-REV_TURN_INNER(32)/-REV_TURN_OUTER(46)` 차동 후진(yaw 부호 보존), 청크 종료 시
   `spin_resume`이 **방향 재판정 없이, 후진 중 회전까지 누적각에 합산**하며 SPIN 재개.
   dead_end(양측 봉쇄)와 BRAKE발 후진은 기존 직후진 유지.
3. **오프축 크루즈 재진입의 조향 권한 부족**: `CENTER_STEER_MAX_ALIGN_PCT 30` 신설 —
   `|heading err| ≥ CENTER_HDG_SLOW_DEG(14°)`일 때만 클램프 해제라 직선 위빙 레짐(±3° 이내)은
   18% 클램프 그대로.

검증: `make -j8`, `make RELEASE=1 -j8`, `git diff --check` 통과. Release 산출물
`build/release/PR_CAR.hex`. 실차 효과는 **[추정]** — §4 체크리스트에서 특히 "코너 진입 후
REVERSE가 회전을 이어가는지"(대시보드 REVERSE 행 steer 부호가 직전 SPIN과 동일)와 코너당
소요시간(3.75s→1.5s 이하 기대)을 볼 것.

### 5.13 고속화 후 주행 안정성 (IMG_3044 ↔ mappv8) — **이전 빌드**

영상 메타데이터: 주행 37.522s(23fps, 480×854), 웹앱 44.800s(30fps, 554×694). 첫 `CORNER`가
양쪽 모두 약 0.5s라 오프셋은 사실상 0s로 보았다. 사용자 요청대로 웹앱은 **0~37.522s만 분석**했고,
주행영상 종료 뒤 7.278s는 판독에서 제외했다.

주요 관측:

| 웹앱 시각 | 텔레메트리와 주행영상 | 판독 |
|---:|---|---|
| 1.5→2.5s | CRUISE heading `+91.2→+101.8→+71.7`, 2.0s `R=7.4cm`, steer `+18` | 코너 직후 잔류 yaw를 고속 CRUISE가 증폭, 축 양쪽으로 약 30° 스윙 |
| 3.5→4.0s | `F=25→5`, CRUISE→REVERSE | 낮춘 stop 24cm로 제동 여유가 사라져 전방 충돌 후 복구 |
| 6.0→6.5s | SIDE_AVOID `-8.4°`→CRUISE `+33.0°`, `L=5.3cm` | 측면이 8cm가 될 때까지 `48/-32` 피벗해 41.4° 과회전 |
| 11.5→12.5s | SIDE_AVOID `+91.5→+67.8`, 이어 `F=4` REVERSE | 회피가 반대 벽/전방 충돌로 이어지는 같은 패턴 |
| 13.5→14.0s | SPIN인데 `F=4,6cm`가 지속 | `spin_open_committed`가 모든 danger를 계속 무시해 실제 접촉도 빔 스윕으로 오판 |
| 35.5→37.5s | SPIN, heading 약 `+92°` 고정, `F=15~17`, `L=3.6~4.5cm` | 전방+측면에 끼인 채 피벗 명령만 유지 |

근본원인과 수정:

1. 최고속도는 `63/68/63/69`로 올렸지만 상태전이 settle이 90ms, 속도도 `58/63/66`이라 사실상
   즉시 고속 복귀했다. **최고속도는 유지**하고 settle을 `650ms, 48/50/52`로 늘렸으며,
   yaw-rate `12~45dps` 구간에만 속도 상한을 `top→38`로 낮췄다.
2. 직진 복귀 시 횡오차 변화 선행 대응과 회전 감쇠가 약했다. lateral KD를 `0.018/0.015→0.030/0.025`,
   yaw 댐퍼를 `0.24,10dps,4%→0.28,8dps,6%`로 보강했다. 3cm 불감대와 ±8° lateral cap은 유지했다.
3. SIDE_AVOID는 측면 clear만 보고 각도 제한이 없었다. IMU 누적 진행각 `18°`까지만 능동 피벗하고,
   이후 스톨 하한 위 `36/36`으로 벽에서 전진 이탈한 뒤 기존 8cm×3 clear로 CRUISE 복귀한다.
4. SPIN open-latch는 단발 짧은 전방 에코만 무시해야 한다. fresh danger 3회 또는
   `F<FRONT_STOP(34) && near side<6` 2회면 latch를 무효화하고 REVERSE로 빠지게 했다.
5. 고속화 실험에서 낮춘 `FRONT_STOP 34→24`는 새 fresh 3회+graze 게이트를 유지한 채 34cm로 복원했다.
   측면 거리 문턱은 올리지 않았다. 전방 샘플은 한 SensorTask 주기당 median 1표만 넣는 현재 변경을 유지한다.

검증: `make -j8`, `make RELEASE=1 -j8`, 코드 대상 `git diff --check` 통과. Release 산출물은
`build/release/PR_CAR.hex`. 실차 결과는 아직 없으므로 위 효과는 **[추정]**이며 §4 체크리스트로 확인할 것.

### 5.12 wide→normal 폴백 + 미사용 graze 게이트 (IMG_3032 ↔ mappv3.mp4) — **이전 빌드**

영상 길이: 실주행 40.035s, 웹앱 45.733s. 시작은 거의 일치하며 첫 `CORNER`가 양쪽 약 0.8s.
정상 첫 코너는 t=0.8 `F30/L24.3/R42.7`, 정상 둘째 코너는 t=2.7
`F36/L43.6/R19.8`로, 열린 쪽 차이가 크면서 가까운 쪽도 15cm 이상이었다. 반면 긴 직선 오발은
t=5.0 `F37/L36.1/R10.5`에서 `CORNER`가 발화했다. 좌우 차이는 25.6cm로 오히려 더 크므로
**좌우 차 문턱만 올리는 것은 해결이 아니며**, 한쪽 벽에 10.5cm로 붙은 직선과 실제 개구부를 구분해야 한다.

근본원인:
1. `wide_corner_context()`가 넓이 판정과 `양쪽>=15cm`를 한 함수에 묶었다. 넓은 직선에서 한쪽이
   15cm 미만으로 붙는 순간 wide가 해제되어 더 느슨한 normal 기준(`near>=7`)으로 폴백했다.
2. `front_graze_suspected()`가 이 경우의 전방 측벽 반사를 올바르게 계산해도 `dbg.graze` 표시만 하고
   실제 코너/제동 게이트에는 사용하지 않았다.
3. §5.11의 `FRONT_WALL_CONFIRM_N=3`도 `front_recent_below()`를 써서 fresh 1회+dropout 래치가
   연속 3회처럼 누적될 수 있었다.

수정:
- 넓이 분류를 near 조건과 분리하고, wide 코너 증거에는 별도로 `near>=15cm`를 강제.
- `graze`이면 danger(12cm) 비상제동은 유지하되 일반 front-wall/STOP/TURN 게이트에서는 제외.
- front-wall 3연속 확인은 `f_valid` fresh 샘플만 누적.
- fresh 전방벽+강한 좌우 형상이 확인된 코너는 마지막 방향확인 프레임부터 `ARC 72/30`을 명령해
  BRAKE 후 피벗 대신 전진하며 턴을 시작. 전방 상실 side-only 경로는 기존처럼 정지 확인.
- 완주 속도를 크게 희생하지 않도록 문제 구간인 wide fast/settle만 `68/66 → 65/63`
  (약 4.4~4.5%)로 하향. 코너 아크 `72/30`과 나머지 속도는 유지.

기대되는 첫 연쇄 차단점은 t=5.0이다. 여기서 새 로직은 wide+near 부족+graze로 `corner_direction=-1`,
front 게이트 억제 후 CRUISE 벽 반발을 계속한다. 기존 영상의 t=5 이후 `CORNER→BRAKE→SPIN→REVERSE`
연쇄는 이 오발의 결과이므로 먼저 이 지점이 사라지는지 실차로 확인할 것.

### 5.11 긴 직선 간헐 오발턴 + SIDE_AVOID 데드락 (IMG_3030 ↔ mappv2.mp4) — **이전 빌드**

관측: (a) 긴 직선에서 살짝 치우침 → 비스듬한 벽 때문에 전방값 1~2프레임 딥 + far측 28.6/33.3cm
개방 → CORNER 오발 → 복구 스핀 (웹앱 t=4.5, 6.5). (b) 종반 벽 4cm 밀착 → SIDE_AVOID 탈출 아크
안쪽 12% duty < 스톨 30% → 스키드에서 회전 불능 → avoid→brake→cruise→재-avoid 10초 무한 루프.

수정: (1) `FRONT_WALL_CONFIRM_N 3` — 코너 게이트는 전방<44cm **3프레임 연속**일 때만.
(2) 개구 문턱 34cm 상향 (오발 far ≤33.3 < 34 ≤ 실코너 33~50; 경계 miss는 §5.9 폴백이 올바른
방향으로 흡수). (3) SIDE_AVOID 탈출 = `CORNER_RESCUE 48/-32` 능동 피벗 (`SIDE_ESCAPE_*` 삭제).

### 직전 수정들의 핵심 (§5.7~5.10 요지 — 자세한 서사는 git 이전 판)

- **위빙 해결 (§5.7~5.8)**: hard deadband는 어느 구성이든 결함 — 합에 걸면 위치 무규제,
  노이즈 항에만 걸면 평형점이 대역 가장자리로 이동. **노이즈는 LPF로, deadband는 0.6° 채터방지만.**
  기준축 스텝(fresh/resnap/IMU 부활) 시 LPF 재시드 필수.
- **전방 스파이크 (§5.9)**: 회전 방향 결정은 **fresh 판독 연속확정**으로만. `front_recent_below`는
  dropout 래치라 스파이크를 유지시킴 — 방향 결정에 쓰지 말 것. BRAKE = 벽 검증 단계
  (벽 증발 → 크루즈 복귀 = 스파이크 무해화).
- **턴 후 사행 (§5.10)**: 크루즈 heading_ref에 45° 스냅 금지 (코너 오버슈트 +115 → ref +135
  대각 고착 → 복도 대각 횡단). ref는 `course_axis_snap`(90°)만. SPIN은 전방 열리면(52cm×3)
  30°부터 탈출 — 88° 강제 회전이 스래시의 원인이었음.

---

## 6. 주행영상 ↔ 웹앱 녹화 교차분석 워크플로 (핵심 작업 방식)

사용자가 두 영상을 준다: **주행영상**(iPhone, 차 추적)과 **웹앱 화면녹화**(같은 주행의 텔레메트리).
길이가 다르다 (웹앱을 먼저 켜고 늦게 끔, 보통 4~5s 김). 이걸로 오발 프레임을 특정해 근본원인을 찾는다.
ffmpeg 없음, **python + opencv(cv2 4.12) 있음**. 산출물은 스크래치 디렉토리에.

### 6.1 절차
1. 메타데이터 확인 (fps/길이/해상도) → 두 영상 길이 차 기록.
2. 웹앱 프레임 1장 떠서 **크롭 좌표 캘리브레이션** (녹화마다 해상도 다름 — 아래 좌표는 참고치).
3. 웹앱을 0.5s 간격으로 크롭-조합한 "텔레메트리 시트" 생성 (13행/장) → 이미지로 판독.
4. 주행영상을 1s 간격 6×3 몽타주로 생성 → 차 위치/자세 판독.
5. **정렬**: 첫 코너(state CORNER 첫 등장 ↔ 영상에서 차가 첫 코너 도달)를 앵커로 오프셋 계산.
6. 상태/Δheading/F/L/R/steer 타임라인을 표로 재구성 → 오발 시점의 센서 값 조합을 실측표(§1)와
   대조 → 판정 로직의 어느 게이트가 뚫렸는지 특정.

### 6.2 웹앱 시트 생성 스크립트 (실사용본, 크롭 좌표만 조정)
```python
import cv2, numpy as np
out = r"<스크래치 경로>"
cap = cv2.VideoCapture(r"c:/Users/user/STM/PR_CAR/<웹앱녹화>.mp4")
rows = []
for t in np.arange(0.0, <길이>, 0.5):
    cap.set(cv2.CAP_PROP_POS_MSEC, t*1000)
    ok, fr = cap.read()
    if not ok: continue
    # 580x710 녹화 기준 크롭 (해상도 다르면 프레임 1장 떠서 재캘리브레이션):
    hdg   = fr[100:225, 150:440]   # Δheading 큰 숫자 + RAW
    lr    = fr[398:435, 10:570]    # 좌/우 mm 행
    front = fr[478:560, 60:260]    # 전방 cm
    spd   = fr[478:565, 295:570]   # 속도 + L/R 엔코더 + steer
    chip  = fr[578:632, 100:500]   # 상태 칩 (CRUISE/CORNER/SPIN/...)
    H = 58
    def rs(img):
        s = H / img.shape[0]
        return cv2.resize(img, (int(img.shape[1]*s), H))
    label = np.zeros((H, 90, 3), np.uint8)
    cv2.putText(label, f"{t:4.1f}", (2, 40), cv2.FONT_HERSHEY_SIMPLEX, 0.9, (0,255,255), 2)
    rows.append(cv2.hconcat([label, rs(chip), rs(hdg), rs(front), rs(spd), rs(lr)]))
cap.release()
W = max(r.shape[1] for r in rows)
rows = [cv2.copyMakeBorder(r, 0, 0, 0, W-r.shape[1], cv2.BORDER_CONSTANT) for r in rows]
for i in range(0, len(rows), 13):
    cv2.imwrite(f"{out}/sheet_{i//13}.png", cv2.vconcat(rows[i:i+13]))
```

### 6.3 주행영상 몽타주 스크립트
```python
cap = cv2.VideoCapture(r"c:/Users/user/STM/PR_CAR/<주행영상>.mov")
tiles = []
for t in np.arange(0.0, <길이>, 1.0):
    cap.set(cv2.CAP_PROP_POS_MSEC, t*1000)
    ok, fr = cap.read()
    if not ok: continue
    fr = cv2.resize(fr, (240, 427))
    cv2.putText(fr, f"{t:.0f}s", (6, 36), cv2.FONT_HERSHEY_SIMPLEX, 1.2, (0,255,255), 3)
    tiles.append(fr)
cap.release()
rowsimg = [cv2.hconcat(tiles[i:i+6]) for i in range(0, len(tiles)-5, 6)]
for j in range(0, len(rowsimg), 3):
    cv2.imwrite(f"{out}/grid_{j//3}.png", cv2.vconcat(rowsimg[j:j+3]))
```

### 6.4 판독 요령
- **Δ HEADING** = raw − 코스 기준축(부팅 방위). 코스축 대비 각: 첫 코너 후 +90, 둘째 후 0,
  마지막 남향 180. CRUISE인데 Δ가 축에서 20°+ 벗어나 있으면 ref 문제.
- **좌/우 mm**를 cm로 환산해 실측표(§1)와 대조. 오발 코너 = 판정 프레임의 far/diff가
  실측 코너 값에 못 미치는데 발화한 경우.
- **속도 우측 `L n R n`** = 엔코더 cm/s (duty 아님). 우측은 현재 항상 0 (하드웨어 이슈).
- **F=80 + FRONT 칩 회색** = 타임아웃(>1m)이지 80cm가 아님.
- 상태 칩이 0.5s 간격에서 CORNER/SPIN/BRAKE로 널뛰면 상태 스래시 — 어떤 게이트가 반복
  발화하는지 각 행의 센서 값으로 역추적.
- 오프셋: 시트의 첫 CORNER 시각 ↔ 몽타주의 첫 코너 도달 시각. 이번 두 세트는 각각 ~1.5s, ~2s였음.

---

## 7. 재발 방지 목록 (코드 수정 전 필독)

1. 헤더 분리 시 매크로 참조 잔존/이중 정의 → 즉시 컴파일 확인.
2. 새 소스는 Makefile + CMakeLists.txt **양쪽** 등록.
3. 구조체 필드 추가 시 모든 생성 지점 초기화 (`DriveInputs` 사례: 스택 쓰레기값이 큐로).
4. LPF와 유효성 상호작용: invalid→valid 첫 샘플은 raw 재초기화 (`left_prev_valid` 패턴).
5. 유효성 플래그 도입 시 모든 소비자 일괄 적용 (한 곳만 고치면 80cm를 개구부로 오독).
6. **duty 명령은 실현 가능 범위 확인**: 직진 (0,30) 불가, 피벗/탈출은 스키드 돌파 듀티 필요.
   sub-stall 안쪽 바퀴 아크로 벽에서 탈출하려던 SIDE_ESCAPE 40/12가 10초 데드락 만든 전례.
   컴파일 가드 활용.
7. 타이머 폴백이 실제 진행도(IMU)를 우회하지 않게; "이번 프레임 누락"≠"이 회전 내내 없음".
8. 확인 카운터에는 방향 포함 (좌1+우1 ≠ 2연속).
9. front-only 이벤트는 루프당 1회, FSM 카운터 접근 금지.
10. **각도 진행은 증분 누적** — `wrap180(now−entry)` 절대 금지 (180° wrap = 방향 반전 버그).
11. 기준값(heading_ref/course_heading) 게이트에는 "기준이 낡았을 때" 예외 경로를 둘 것.
12. CubeMX regen: USER CODE 블록 밖 코드 소멸. `.ioc` = 소스오브트루스 (BT 스택 384, TIM2 PSC).
13. CRLF repo — `git diff --check` 습관화. graphify-out/은 생성물이라 whitespace 경고 무시.
14. 텔레메트리 인덱스/DS_ enum 값 = 웹앱 계약. 변경 금지, 추가는 append.
15. **hard deadband로 노이즈를 막지 말 것** — LPF로 막고 deadband는 채터방지 소폭만 (§5.8 교훈).
16. **회전 방향 결정에 래치/스파이크 값 금지** — fresh 판독 연속확정만 (§5.9 교훈).
17. **크루즈 heading_ref는 90° 코스축만** — 45° 스냅은 대각 고착 사고 전례 (§5.10 교훈).
18. 코너 판정 문턱을 내리기 전에 "치우친 직선"이 그 값을 만들 수 있는지 실측표(§1)와 대조 (§5.11 교훈).
19. **피벗 중단 후진은 3점턴** — 직후진+크립 재접근은 같은 포켓 포즈를 복원하고 진행각을 리셋한다.
    `spin_reverse_enter`/`spin_resume` 경로 유지, 방향 재판정 금지 (§5.14 교훈).
20. **코스반전(≥180°) 예외는 `turn_leg_deg`(끊김 없는 단일 회전)만 본다** — 후진 청크로 이어붙인
    누적(`turn_accum_deg`)으로 판정하면 일반 코너 싸움이 역주행으로 진출한다 (§5.16 교훈).
    SPIN의 CRUISE 진출 경로를 새로 만들면 반드시 course_dev 게이트를 같이 달 것.

## 8. 남은 문제 (우선순위순)

0. **§5.17 v2 코어 실차 검증** + **90° ToF 기준 §1 실측표 재작성** (수동주행 1회) →
   [재실측] 표시 상수 조정. 이후 §5.13 이하 체크리스트.
1. **§5.13 Release 실차 검증** (체크리스트 §4). 실패 시 영상 2종 + §6 워크플로로 분석.
2. **첫 목표는 무충돌 완주**: 안정 직선 최고속도는 이미 상향됐으므로, 다음 튜닝은 settle/yaw/회피
   동작을 검증한 뒤에만 한다. 20s 목표를 위해 추가 상향하더라도 한 번에 5% 이내.
3. **방지턱(67cm 구간)**: 턱 위 스톨/피치업으로 전방이 바닥/허공 → 근본 해결 = 엔코더 속도 PI
   (R4 계획, `#VT` 저장만 됨) + [추정] IMU pitch 게이트. **선행: 우측 엔코더 0 출력 하드웨어 수리.**
4. 전방 초음파 저장착: 바닥 에코 = 소프트웨어 구분 불가. 직진 중 산발 가짜 BRAKE가 §5.9 검증으로
   무해화됐지만 빈발하면 장착 높이/각도부터 확인.
5. BNO055 heading 실내 드리프트 — 절대값 의존 로직 추가 금지, 차분/단기 참조만.

## 9. 불변식 (깨면 조용히 망가짐)

- `FRONT_DANGER < FRONT_DECIDE < FRONT_STOP < FRONT_TURN < FRONT_CLEAR < FRONT_ARC` (가드 있음)
- `ARC_INNER, ARC_APPROACH_INNER, TURN_INNER, TURN_PID_*_MIN, CORNER_RESCUE_*, BRAKE_CREEP_PCT
  ≥ MOTOR_MIN_PCT` (가드 있음)
- `CORNER_ABORT < CORNER_TIGHTEN < FRONT_CLEAR` (가드 있음)
- ~~`CENTER_LATERAL_CMD_MAX_DEG(11°) < CORNER_ENTRY_HDG_MAX_DEG(12°)`~~ **폐기(§5.19)**:
  12° graze 밴드는 45° 장착 삼각법의 산물, v2에서 상수 자체 삭제됨. lateral 캡 15°.
- 회전 진행각 누적 방식 유지 (§7-10)
- IMU 의존 게이트에는 IMU-무관 백스톱 (`SPIN_BLIND_MS 650`, `LAUNCH_MS 120`) — 모터 기동
  인러시 브라운아웃으로 imu_live가 죽는 하드웨어 특성
- MotorTask만 모터 명령 발행 (예외: 폴트 핸들러 Car_Stop)
- 크루즈 heading_ref = `course_axis_snap`(90°)만. BNO055 절대 방위 금지, 부팅 상대축만.
